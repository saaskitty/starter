import type { App } from "#app/.server/main.js";

/**
 * Get the application cron jobs.
 *
 * @param {App} app The app instance.
 * @returns {Object} An object containing the cron jobs.
 */
export function getCrons(app: App) {
	return {};
}
