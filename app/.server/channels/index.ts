/**
 * Get the application channels powered by websocket.
 *
 * @param {App} app The app instance.
 * @returns {Object} An object containing the websocket channels.
 */
export function getChannels() {
	return {};
}
