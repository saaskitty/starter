import type { App } from "#app/.server/main.js";

/**
 * Get the application background jobs.
 *
 * @param {App} app The app instance.
 * @returns {Object} An object containing the background jobs.
 */
export function getJobs(app: App) {
	return {};
}
