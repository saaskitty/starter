import type { App } from "#app/.server/main.js";

/**
 * Get the application services.
 *
 * @param {App} app The app instance.
 * @returns {Object} An object containing the application services.
 */
export function getServices(app: App) {
	return {};
}
