import type { App } from "#app/.server/main.js";

/**
 * Get the application events.
 *
 * @param {App} app The app instance.
 * @returns {Object} An object containing the events.
 */
export function getEvents(app: App) {
	return {};
}
