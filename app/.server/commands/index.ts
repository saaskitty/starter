import type { App } from "#app/.server/main.js";

/**
 * Get the application commands.
 *
 * @param {App} app The app instance.
 * @returns {Object} An object containing the commands.
 */
export function getCommands(app: App) {
	return [];
}
